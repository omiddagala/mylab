all:
  vars:
    ansible_user: debian
    ansible_python_interpreter: /usr/bin/python3

  children:
    hypervisors:
      hosts:
        localhost:
          ansible_connection: local
          ansible_become: true
          ansible_python_interpreter: /usr/bin/python3