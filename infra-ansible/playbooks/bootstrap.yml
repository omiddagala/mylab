---
- name: OS bootstrap baseline
  hosts: all
  become: true
  roles:
    - common_base
    - apt_baseline
    - users_admin
    - ssh_hardening
    - time_sync
    - journald
    - sysctl_tuning
    - nftables_base
