---
- name: Bootstrap Linux hosts
  hosts: linux
  gather_facts: true
  become: true
  roles:
    - common_base
    - apt_baseline
    - users_admin
    - ssh_hardening
    - time_sync
    - journald
    - sysctl_tuning
